mixin textfield(options)
    - const { title = "", placeholder = "", value = "" } = options;
    if title
        h3.text-field__title= title
    input.text-field(type='text', placeholder=placeholder, value=value)
