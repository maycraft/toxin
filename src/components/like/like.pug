mixin like(options)
    -
        const { count, isLiked } = options
        let classLike = 'like'
        classLike += isLiked ? ' like-clicked' : ''
    button(class=classLike)
        span.like__count= count
